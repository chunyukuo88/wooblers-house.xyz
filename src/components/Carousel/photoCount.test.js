import { getFotoCount } from './photoCount';

describe('photoCount.js', ()=>{
  describe('getFotoCount()', ()=>{
    describe('WHEN: Invoked,', ()=>{
      it('THEN: The function returns the number of items in the S3 bucket.', ()=>{

      });
    });
  });
});
